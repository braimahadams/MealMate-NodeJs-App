pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/braimah/meal-mate.git'
            }
        }

        stage('Build') {
            steps {
                sh 'echo "Hello World"'
            }
        }

        stage('Test') {
            steps {
            
            }
        }

        stage('Deploy') {
            steps {

            }
        }
    }
}

